# 奥迪新媒体运营 AI 助手 (Social Media AI Ops Pro)

这是一个专门为一汽奥迪经销商设计的专业新媒体运营平台。集成了基于 Google Gemini 2.5/3.0 系列模型的 AI 诊断、脚本生成、文案提炼及案例萃取功能。

## 🚀 快速上手

### 1. 环境准备
确保您的本地环境安装了：
- [Node.js](https://nodejs.org/) (建议 v18.0.0 以上)
- [Git](https://git-scm.com/)

### 2. 获取代码
将此项目的所有文件保存到您的本地文件夹中（例如 `audi-ai-assistant`）。

### 3. 安装依赖
在项目根目录下运行：
```bash
npm install
```

### 4. 配置 API Key
项目依赖于 Google Gemini API。在运行前，请确保设置了环境变量：
- 在 Windows (PowerShell): `$env:API_KEY="您的密钥"`
- 在 Mac/Linux: `export API_KEY="您的密钥"`

### 5. 启动开发服务器
```bash
npm run dev
```

## 📂 核心功能
- **运营诊断**：基于 5s 完播率、线索成本等指标进行门店经营健康度分析。
- **脚本助手**：支持“对标素材上传”，自动生成包含 10 组专业分镜的短视频脚本。
- **案例萃取**：支持上传视频/图片，AI 自动拆解爆款逻辑。
- **笔记实验室**：小红书爆款正文、标题及 SEO 标签的一键生成。

## ⬆️ 如何上传到 GitHub

如果您想将此项目保存到您的 GitHub 仓库，请按照以下步骤操作：

1. **在 GitHub 上新建仓库**：
   - 登录 GitHub，点击右上角 `+` -> `New repository`。
   - 命名为 `audi-social-media-ai-assistant`，点击 `Create repository`。

2. **在本地文件夹执行命令**：
   打开终端或 CMD，进入项目根目录：
   ```bash
   # 初始化本地 Git 仓库
   git init

   # 添加所有文件到暂存区
   git add .

   # 提交更改
   git commit -m "Initial commit: Audi Social Media AI Assistant"

   # 创建主分支
   git branch -M main

   # 关联远程仓库 (请替换下面的 URL 为您刚才新建仓库的地址)
   git remote add origin https://github.com/您的用户名/audi-social-media-ai-assistant.git

   # 推送到 GitHub
   git push -u origin main
   ```

## 🛠 技术栈
- **Frontend**: React 19, Tailwind CSS, Lucide React
- **AI Engine**: Google Generative AI (Gemini API)
- **Environment**: Vite / Esbuild

---
*本项目仅供奥迪门店内部运营参考使用。*
